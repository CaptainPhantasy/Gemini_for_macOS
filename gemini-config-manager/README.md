# Gemini Config Manager

This is a standalone mini-app for managing Gemini configuration and themes.

## Purpose

It provides a visual interface for editing theme variables and configuration settings that are used by the main GEMINI for MacOS application.

## Usage

Open `index.html` in a browser to use the configuration manager.
